<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ecommerce.order.infrastructure.persistence.mybatis.OrderMapper">

  <resultMap id="OrderSummaryMap" type="com.ecommerce.order.interfaces.dto.OrderSummary">
    <result property="orderId"     column="order_id"/>
    <result property="userId"      column="user_id"/>
    <result property="username"    column="username"/>
    <result property="totalAmount" column="total_amount"/>
    <result property="status"      column="status"/>
    <result property="createdAt"   column="created_at"/>
  </resultMap>

  <select id="searchOrders" resultMap="OrderSummaryMap">
    SELECT o.id AS order_id, o.user_id, u.username,
           o.total_amount, o.status, o.created_at
    FROM orders o
    LEFT JOIN users u ON o.user_id = u.id
    <where>
      <if test="query.userId != null">
        AND o.user_id = #{query.userId}
      </if>
      <if test="query.status != null and query.status != ''">
        AND o.status = #{query.status}
      </if>
      <if test="query.startDate != null">
        AND o.created_at >= #{query.startDate}
      </if>
      <if test="query.endDate != null">
        AND o.created_at &lt;= #{query.endDate}
      </if>
    </where>
    ORDER BY o.created_at DESC
    LIMIT #{query.size} OFFSET #{query.page}
  </select>

</mapper>
